include "std/jdk"
include "std/list"
include "std/console"

namespace Main {
    command main args:array<string> -> void {
        let arr = (std.list.init)
        std.console.log arr
        std.console.log 15
    }

    command fib n:number -> number {
        return n
    }
}